export function AppFooter() {
  return (
    <footer className="ftr small muted">
      Nominatim (geocoding) + project-osrm.org (routing) + OpenStreetMap tiles. Deploy backend
      (Django) and static frontend; see README.
    </footer>
  )
}
